export interface Handwriting {
  handwritingId: number;
  name: string;
  state: number;
  likeCount: number;
  downloadCount: number;
  downloadUrl: string;
  createDate: string;
  tags: number[];
}
