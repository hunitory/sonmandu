'use client';

import { PALETTE } from 'styles';
import styled from 'styled-components';

export const FontCreateMainWrapper = styled.main`
  width: 100vw;
  height: 100vh;
`;


export const FontCreateMainContainer = styled.main`
`;
